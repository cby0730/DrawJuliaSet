暫存器規劃
r0  = temp
r1  = temp
r2  = temp
r3  = temp
r4  = frame
r5  = x
r6  = y
r7  = i, color
r8  = zx
r9  = zy
r10 = tmp
r11 = cY
------------------------------------------------------------------------
除法：在呼叫__aeabi_idiv前要先設定，r0為被除數，r1為除數。return回來的答      案會放在r0，雖然看起來只動到r0r1，但r2r3也有可能有變化。所以r0到r4       盡量不要拿來存東西，只能放一些短暫運算的值

     除法前面是兩個底線，後面是一個
-----------------------------------------------------------------------
.constant : 常數池，這邊都是一些不是立即值的數
-----------------------------------------------------------------------
二維陣列 : frame[a][b] = frame + 1280*a + 2*b 
           1280 = 640 * 2，2是因為這是int16_t，只有int的一半，
           int = 1word = 4byte，int16_t = 2byte
-----------------------------------------------------------------------
color = (~color)&0xffff;  -> 請去看課本裡 bic 的用法